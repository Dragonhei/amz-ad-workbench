[pytest]
testpaths = tests
python_files = test_*.py
addopts = -q
filterwarnings =
    ignore::DeprecationWarning
    ignore::PendingDeprecationWarning
# 覆盖率由 `coverage run -m pytest --no-cov && coverage report` 生成（见 CI T8）。
# 不使用 pytest-cov 的 --cov，因沙箱 safe-delete 钩子会拦截其收尾删除 .coverage.* 临时文件。
